由于您的电脑不能联网，我将提供一个纯标准库的Python脚本，不需要安装任何额外模块：

```python
import tkinter as tk
import subprocess
import os
import sys
from tkinter import messagebox

def close_applications():
    """使用Windows系统命令关闭指定应用程序"""
    applications = {
        "Edge浏览器": "msedge.exe",
        "记事本": "notepad.exe",
        "CMD窗口": "cmd.exe"
    }
    
    results = []
    
    for app_name, process_name in applications.items():
        try:
            # 使用taskkill命令结束进程
            # /f 强制结束，/im 指定进程名
            result = subprocess.run(
                ["taskkill", "/f", "/im", process_name],
                capture_output=True,
                text=True,
                timeout=5
            )
            
            if "SUCCESS" in result.stdout or "成功" in result.stdout:
                results.append(f"✓ {app_name} 已关闭")
            elif "没有运行" in result.stdout or "not running" in result.stdout:
                results.append(f"○ {app_name} 未运行")
            else:
                results.append(f"✗ {app_name} 关闭失败")
                
        except subprocess.TimeoutExpired:
            results.append(f"✗ {app_name} 操作超时")
        except Exception as e:
            results.append(f"✗ {app_name} 错误: {str(e)[:30]}")
    
    # 更新结果显示
    result_text = "\n".join(results)
    result_label.config(text=result_text)
    
    # 记录到日志文件（可选）
    log_result("\n".join(results))

def log_result(message):
    """记录操作日志到文件"""
    try:
        with open("close_apps_log.txt", "a", encoding="utf-8") as f:
            from datetime import datetime
            timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
            f.write(f"[{timestamp}] {message}\n")
    except:
        pass

def on_double_click(event):
    """双击事件处理"""
    # 确认对话框
    if messagebox.askyesno("确认", "确定要关闭以下应用程序吗？\n\n• Microsoft Edge浏览器\n• 记事本\n• 命令提示符"):
        close_applications()

def create_gui():
    """创建GUI界面"""
    global root, result_label
    
    root = tk.Tk()
    root.title("应用程序关闭工具 - 双击使用")
    root.geometry("500x400")
    
    # 设置窗口居中
    window_width = 500
    window_height = 400
    screen_width = root.winfo_screenwidth()
    screen_height = root.winfo_screenheight()
    x = (screen_width - window_width) // 2
    y = (screen_height - window_height) // 2
    root.geometry(f"{window_width}x{window_height}+{x}+{y}")
    
    # 设置窗口图标（可选，使用内置图标）
    try:
        root.iconbitmap(default=None)  # 使用系统默认图标
    except:
        pass
    
    # 主标题
    title_label = tk.Label(
        root, 
        text="应用程序快速关闭工具",
        font=("Arial", 16, "bold"),
        fg='#2C3E50'
    )
    title_label.pack(pady=20)
    
    # 图标区域（使用文本符号模拟图标）
    icon_frame = tk.Frame(root)
    icon_frame.pack(pady=10)
    
    # 创建三个应用图标
    apps = [
        ("🌐", "Edge浏览器"),
        ("📝", "记事本"),
        ("💻", "CMD窗口")
    ]
    
    for icon, name in apps:
        app_frame = tk.Frame(icon_frame)
        app_frame.pack(side=tk.LEFT, padx=15)
        
        icon_label = tk.Label(
            app_frame,
            text=icon,
            font=("Arial", 24)
        )
        icon_label.pack()
        
        name_label = tk.Label(
            app_frame,
            text=name,
            font=("Arial", 10)
        )
        name_label.pack()
    
    # 分隔线
    separator = tk.Frame(root, height=2, bg="#BDC3C7")
    separator.pack(fill=tk.X, padx=40, pady=20)
    
    # 双击区域
    click_area = tk.Frame(
        root,
        bg='#3498DB',
        height=80,
        relief=tk.RAISED,
        borderwidth=2
    )
    click_area.pack(pady=20, padx=40, fill=tk.X)
    
    # 双击区域内的文字
    click_label = tk.Label(
        click_area,
        text="🖱️ 双击此处关闭应用程序 🖱️",
        font=("Arial", 14, "bold"),
        bg='#3498DB',
        fg='white'
    )
    click_label.pack(expand=True)
    
    # 绑定双击事件
    click_area.bind("<Double-Button-1>", on_double_click)
    click_label.bind("<Double-Button-1>", on_double_click)
    
    # 结果显示区域
    result_frame = tk.Frame(root)
    result_frame.pack(pady=10, padx=40, fill=tk.BOTH, expand=True)
    
    result_label = tk.Label(
        result_frame,
        text="等待操作...",
        font=("Arial", 11),
        bg='#ECF0F1',
        fg='#2C3E50',
        wraplength=400,
        justify=tk.LEFT,
        relief=tk.SUNKEN,
        padx=10,
        pady=10
    )
    result_label.pack(fill=tk.BOTH, expand=True)
    
    # 底部说明
    bottom_frame = tk.Frame(root)
    bottom_frame.pack(side=tk.BOTTOM, pady=10)
    
    info_label = tk.Label(
        bottom_frame,
        text="提示: 此操作将强制关闭所有指定的应用程序，请确保已保存重要数据",
        font=("Arial", 8),
        fg='#7F8C8D'
    )
    info_label.pack()
    
    # 让整个窗口都支持双击
    root.bind("<Double-Button-1>", on_double_click)
    
    # 添加键盘快捷键支持 (ESC退出, F5刷新)
    root.bind('<Escape>', lambda e: root.destroy())
    root.bind('<F5>', lambda e: result_label.config(text="等待操作..."))
    
    # 运行GUI
    root.mainloop()

def main():
    """主函数"""
    print("正在启动应用程序关闭工具...")
    print("提示: 双击界面任意位置即可关闭指定应用程序")
    print("-" * 50)
    
    # 检查是否是Windows系统
    if sys.platform != "win32":
        print("错误: 此工具仅适用于Windows系统")
        input("按任意键退出...")
        return
    
    # 检查taskkill命令是否可用
    try:
        subprocess.run(["taskkill", "/?"], capture_output=True, timeout=2)
    except:
        print("警告: 无法执行系统命令，工具可能无法正常工作")
    
    create_gui()

if __name__ == "__main__":
    main()
```

使用方法：

1. 保存脚本

1. 打开记事本（Notepad）
2. 复制上面的全部代码
3. 粘贴到记事本中
4. 点击"文件" → "另存为"
5. 文件名输入：close_apps.py
6. 保存类型选择：所有文件 (*.*)
7. 编码选择：UTF-8
8. 点击"保存"

2. 运行脚本

1. 找到保存的 close_apps.py 文件
2. 双击运行（如果Python已正确安装）
3. 或者，打开命令提示符(cmd)，切换到文件所在目录，运行：
   ```
   python close_apps.py
   ```

3. 使用界面

1. 程序启动后会显示一个蓝色区域
2. 双击蓝色区域或任意位置
3. 会弹出确认对话框
4. 点击"是"即可关闭：
   · Microsoft Edge浏览器
   · 记事本
   · 命令提示符(cmd)

4. 额外功能

· ESC键: 退出程序
· F5键: 清空结果
· 日志记录: 操作记录会自动保存到 close_apps_log.txt 文件

创建可执行文件（可选）

如果您希望直接双击运行而无需Python环境，可以使用以下方法：

方法1：使用Python自带的工具（需要同一台电脑）

```bash
# 在命令提示符中运行：
python -m PyInstaller --onefile --windowed close_apps.py
```

方法2：如果无法安装PyInstaller

您可以创建一个批处理文件来简化操作：

1. 创建一个新文本文件
2. 输入以下内容：

```batch
@echo off
echo 正在启动应用程序关闭工具...
python "%~dp0close_apps.py"
pause
```

1. 保存为 run_app.bat（与Python脚本同一目录）
2. 双击 run_app.bat 即可运行

注意事项

1. 权限问题: 该工具使用标准的Windows命令，通常不需要管理员权限
2. 数据安全: 强制关闭应用程序可能导致未保存的数据丢失
3. 进程结束: 会结束所有同名的进程（如多个Edge窗口）
4. 兼容性: 仅在Windows系统上有效

故障排除

如果遇到问题，可以尝试：

1. Python未安装:
   · 按 Win + R，输入 python --version
   · 如果没有显示版本号，需要安装Python
2. 脚本无法运行:
   · 确保文件扩展名是 .py 而不是 .txt
   · 右键文件 → 打开方式 → 选择Python
3. 无法关闭应用程序:
   · 尝试手动在命令提示符中运行：taskkill /f /im msedge.exe

这个脚本完全使用Python标准库，不需要任何额外安装，非常适合不能联网的电脑使用。